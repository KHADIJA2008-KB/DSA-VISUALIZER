'use client'

import { useEffect, useState } from 'react'
import { DEFAULT_DEQUE_CAPACITY, dequeIsEmpty, dequeIsFull, insertBack, insertFront, peekBack, peekFront, removeBack, removeFront } from '@/lib/algorithms/queue/variants'
import { StepControls } from '@/lib/visualizer/StepControls'
import { useStepPlayer } from '@/lib/visualizer/useStepPlayer'
import type { Step } from '@/lib/visualizer/types'

const initialDeque = [18, 42, 27]
const labels: Record<string, string> = { insertFront: 'Inserted at front', insertBack: 'Inserted at back', removeFront: 'Removed from front', removeBack: 'Removed from back', peekFront: 'Peeked at front', peekBack: 'Peeked at back', empty: 'Deque is empty', notEmpty: 'Deque has values', full: 'Deque is full', notFull: 'Deque has space' }

function description(step?: Step) { if (!step) return 'Choose an operation to create a step.'; const label = labels[step.type] ?? step.type; return step.value === undefined ? label : `${label}: ${step.value}` }

export function DequeVisualizer() {
  const [deque, setDeque] = useState(initialDeque)
  const [steps, setSteps] = useState<Step[]>([])
  const [inputValue, setInputValue] = useState('64')
  const player = useStepPlayer(steps, 320)
  const activeStep = player.currentStep >= 0 ? steps[player.currentStep] : undefined
  const visibleDeque = activeStep?.array ?? deque
  const play = player.play

  useEffect(() => { if (steps.length > 0) play() }, [steps, play])

  function playOperation(nextSteps: Step[]) { setSteps(nextSteps); const finalArray = nextSteps[nextSteps.length - 1]?.array; if (finalArray) setDeque(finalArray) }
  function value() { return Number(inputValue) }

  return <div className="mt-12 grid gap-5 xl:grid-cols-[minmax(0,1fr)_320px]"><section className="overflow-hidden rounded-2xl border border-slate-800 bg-slate-950 p-5 shadow-2xl shadow-slate-300/30 dark:shadow-black/30 sm:p-7" aria-label="deque visualizer"><div className="flex flex-col gap-4 border-b border-slate-800 pb-5"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-300">Double-ended queue</p><p className="mt-1 text-sm text-slate-500">Insert or remove from either end · {visibleDeque.length} / {DEFAULT_DEQUE_CAPACITY} slots used</p></div><div className="flex flex-wrap items-end gap-2"><label className="text-xs text-slate-500">Value<input type="number" value={inputValue} onChange={(event) => setInputValue(event.target.value)} className="mt-1 block w-20 rounded-lg border border-slate-700 bg-slate-900 px-3 py-2 text-sm text-white outline-none focus:border-violet-400" /></label><button type="button" onClick={() => playOperation(insertFront(deque, value()))} className="rounded-lg bg-violet-300 px-3 py-2 text-xs font-bold text-slate-950 transition hover:bg-violet-200">Insert front</button><button type="button" onClick={() => playOperation(insertBack(deque, value()))} className="rounded-lg border border-slate-700 px-3 py-2 text-xs font-semibold text-slate-300 transition hover:border-violet-400 hover:text-white">Insert back</button></div></div><div className="mt-8 overflow-x-auto rounded-xl border border-slate-800 bg-slate-900/70 p-4 sm:p-6"><div className="mx-auto flex min-w-[42rem] items-end justify-center gap-3">{Array.from({ length: DEFAULT_DEQUE_CAPACITY }, (_, index) => { const item = visibleDeque[index]; const isActive = activeStep?.index === index; return <div key={index} className="flex w-24 shrink-0 flex-col gap-2"><div className="h-8 text-center text-[10px] font-semibold uppercase tracking-widest text-slate-500">{index === 0 ? 'front' : index === visibleDeque.length - 1 && visibleDeque.length > 0 ? 'back' : ''}</div><div className={`flex h-16 items-center justify-center rounded-lg border font-mono text-lg transition ${item === undefined ? 'border-dashed border-slate-700 text-slate-700' : isActive ? 'border-violet-300 bg-violet-300 text-slate-950' : 'border-violet-400/70 bg-violet-400/10 text-violet-100'}`}>{item ?? '·'}</div><span className="text-center text-xs text-slate-600">slot {index}</span></div>})}</div>{visibleDeque.length === 0 && <p className="mt-6 text-center text-sm text-slate-600">Deque is empty</p>}</div><div className="mt-5 grid grid-cols-2 gap-2 sm:grid-cols-4"><button type="button" onClick={() => playOperation(removeFront(deque))} className="rounded-lg border border-slate-700 px-3 py-2 text-xs font-semibold text-slate-300 transition hover:border-violet-400 hover:text-white">Remove front</button><button type="button" onClick={() => playOperation(removeBack(deque))} className="rounded-lg border border-slate-700 px-3 py-2 text-xs font-semibold text-slate-300 transition hover:border-violet-400 hover:text-white">Remove back</button><button type="button" onClick={() => playOperation(peekFront(deque))} className="rounded-lg border border-slate-700 px-3 py-2 text-xs font-semibold text-slate-300 transition hover:border-violet-400 hover:text-white">Peek front</button><button type="button" onClick={() => playOperation(peekBack(deque))} className="rounded-lg border border-slate-700 px-3 py-2 text-xs font-semibold text-slate-300 transition hover:border-violet-400 hover:text-white">Peek back</button></div><div className="mt-6"><StepControls {...player} currentStep={player.currentStep} totalSteps={steps.length} /></div></section><aside className="overflow-hidden rounded-2xl border border-slate-800 bg-[#111827] shadow-xl" aria-label="deque status"><div className="border-b border-slate-800 px-5 py-4"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-slate-500">Operations</p><p className="mt-1 text-sm font-semibold text-white">dequeOps.ts</p></div><div className="grid gap-2 p-4"><button type="button" onClick={() => playOperation(dequeIsEmpty(deque))} className="rounded-lg border border-slate-700 px-3 py-2 text-left font-mono text-xs text-slate-300 transition hover:border-violet-400 hover:text-white">isEmpty()</button><button type="button" onClick={() => playOperation(dequeIsFull(deque))} className="rounded-lg border border-slate-700 px-3 py-2 text-left font-mono text-xs text-slate-300 transition hover:border-violet-400 hover:text-white">isFull()</button></div><div className="border-t border-slate-800 px-5 py-4 text-xs text-slate-500">{description(activeStep)}</div></aside></div>
}
